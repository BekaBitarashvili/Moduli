// Language translations
const translations = {
    en: {
        "Home": "Home",
        "Projects": "Projects",
        "Partners": "Partners",
        "Contact": "Contact",
        "The Annual Conference 2017": "The Annual Conference 2017",
        "December 20-29, 2017": "December 20-29, 2017",
        "We are appearing with something amazing so stay updated": "We are appearing with something amazing so stay updated",
        "Day": "Day",
        "hr": "hr",
        "min": "min",
        "sec": "sec",
        "EVENT SHOW": "EVENT SHOW",
        "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed diam nonummy nibh euismod tincidunt ut laoreet dolore magna aliquam erat volutpat.": "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed diam nonummy nibh euismod tincidunt ut laoreet dolore magna aliquam erat volutpat.",
        "New York: Christmas Event Live Music": "New York: Christmas Event Live Music",
        "New York: Exclusive Event DJ Party": "New York: Exclusive Event DJ Party",
        "New York: Children Event Dance Show": "New York: Children Event Dance Show",
        "REGISTRATION NOW": "REGISTRATION NOW",
        "Your Name": "Your Name",
        "Your Email": "Your Email",
        "Your Phone": "Your Phone",
        "Register Now": "Register Now",
        "OUR GREAT SPONSORS": "OUR GREAT SPONSORS",
        "GET IN TOUCH WITH US": "GET IN TOUCH WITH US",
        "Full Name*": "Full Name*",
        "Email*": "Email*",
        "Phone*": "Phone*",
        "Subject*": "Subject*",
        "Message*": "Message*",
        "Send Now": "Send Now",
        "Blog": "Blog",
        "Tickets": "Tickets",
        "FAQ": "FAQ",
        "Press": "Press",
        "All Rights Reserved": "All Rights Reserved",
        "Designed By": "Designed By",
        "TEST": "TEST",
        "First Partner: Full Name": "First Partner: Full Name",
        "Work Gallery": "Work Gallery",
        "Test Button": "Test Button",
        "Work Hours - 9:00 - 18:00": "Work Hours - 9:00 - 18:00",
        "Azerbaijan, Baku": "Azerbaijan, Baku",
        "October 20-30, 2025": "October 20-30, 2025",
        "$75 000 - $200 000": "$75 000 - $200 000",
        "FAMILY DANCE SHOW": "FAMILY DANCE SHOW",
        "New York: Children Event Dance Show": "New York: Children Event Dance Show",
        "A magical dance extravaganza designed especially for children and families! Join us for an enchanting performance featuring colorful costumes, uplifting music, and interactive dance experiences.": "A magical dance extravaganza designed especially for children and families! Join us for an enchanting performance featuring colorful costumes, uplifting music, and interactive dance experiences.",
        "February 10-11, 2018": "February 10-11, 2018",
        "2:00 PM - 5:00 PM": "2:00 PM - 5:00 PM",
        "Lincoln Center, NYC": "Lincoln Center, NYC",
        "$25 - $80": "$25 - $80",
        "Get Family Tickets": "Get Family Tickets",
        "Services": "Services",
        "Our Core Services": "Our Core Services",
        "We offer a comprehensive suite of services to drive your business forward.": "We offer a comprehensive suite of services to drive your business forward.",
        "Software Development": "Software Development",
        "Custom software solutions tailored to your unique business needs, ensuring efficiency and scalability.": "Custom software solutions tailored to your unique business needs, ensuring efficiency and scalability.",
        "Digital Strategy & Marketing": "Digital Strategy & Marketing",
        "Driving growth through data-backed digital strategies, SEO, content, and social media management.": "Driving growth through data-backed digital strategies, SEO, content, and social media management.",
        "UI/UX Design & Branding": "UI/UX Design & Branding",
        "Creating intuitive and engaging user experiences and developing strong, memorable brand identities.": "Creating intuitive and engaging user experiences and developing strong, memorable brand identities.",
        "Cloud & Infrastructure": "Cloud & Infrastructure",
        "Modernizing your IT infrastructure with secure, scalable cloud solutions and robust managed services.": "Modernizing your IT infrastructure with secure, scalable cloud solutions and robust managed services.",
        "Learn More": "Learn More"
    },
    ka: {
        "Home": "მთავარი",
        "Projects": "პროექტები",
        "Partners": "პარტნიორები",
        "Contact": "კონტაქტი",
        "The Annual Conference 2017": "წლიური კონფერენცია 2017",
        "December 20-29, 2017": "20-29 დეკემბერი, 2017",
        "We are appearing with something amazing so stay updated": "ჩვენ ვჩნდებით რაღაც საოცარი რამით, ასე რომ იყავით განახლებული",
        "Day": "დღე",
        "hr": "სთ",
        "min": "წთ",
        "sec": "წმ",
        "EVENT SHOW": "ღონისძიების ჩვენება",
        "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed diam nonummy nibh euismod tincidunt ut laoreet dolore magna aliquam erat volutpat.": "ლორემ იპსუმ დოლორ სით ამეტ, კონსექტეტურ ადიპისცინგ ელით, სედ დიამ ნონუმი ნიბჰ ეუისმოდ ტინციდუნტ უტ ლაორეტ დოლორე მაგნა ალიკუამ ერატ ვოლუტპატ.",
        "New York: Christmas Event Live Music": "ნიუ-იორკი: საშობაო ღონისძიება ლაივ მუსიკა",
        "New York: Exclusive Event DJ Party": "ნიუ-იორკი: ექსკლუზიური ღონისძიება DJ პარტი",
        "New York: Children Event Dance Show": "ნიუ-იორკი: ბავშვთა ღონისძიება ცეკვის შოუ",
        "REGISTRATION NOW": "რეგისტრაცია ახლა",
        "Your Name": "თქვენი სახელი",
        "Your Email": "თქვენი ელ-ფოსტა",
        "Your Phone": "თქვენი ტელეფონი",
        "Register Now": "რეგისტრაცია ახლა",
        "OUR GREAT SPONSORS": "ჩვენი შესანიშნავი სპონსორები",
        "GET IN TOUCH WITH US": "დაუკავშირდით ჩვენ",
        "Full Name*": "სრული სახელი*",
        "Email*": "ელ-ფოსტა*",
        "Phone*": "ტელეფონი*",
        "Subject*": "თემა*",
        "Message*": "შეტყობინება*",
        "Send Now": "გაგზავნა ახლა",
        "Blog": "ბლოგი",
        "Tickets": "ბილეთები",
        "FAQ": "FAQ",
        "Press": "პრესა",
        "All Rights Reserved": "ყველა უფლება დაცულია",
        "Designed By": "დიზაინი",
        "TEST": "ტესტი",
        "First Partner: Full Name": "პირველი პარტნიორი: სრული სახელი",
        "Work Gallery": "სამუშაო გალერეა",
        "Test Button": "ტესტ ღილაკი",
        "Work Hours - 9:00 - 18:00": "სამუშაო საათები - 9:00 - 18:00",
        "Azerbaijan, Baku": "აზერბაიჯანი, ბაქო",
        "October 20-30, 2025": "20-30 ოქტომბერი, 2025",
        "$75 000 - $200 000": "$75 000 - $200 000",
        "FAMILY DANCE SHOW": "ოჯახური ცეკვის შოუ",
        "New York: Children Event Dance Show": "ნიუ-იორკი: ბავშვთა ღონისძიება ცეკვის შოუ",
        "A magical dance extravaganza designed especially for children and families! Join us for an enchanting performance featuring colorful costumes, uplifting music, and interactive dance experiences.": "ჯადოსნური ცეკვის ექსტრავაგანზა, რომელიც განკუთვნილია ბავშვებისა და ოჯახებისთვის! შემოგვიერთდით მომხიბვლელი წარმოდგენისთვის ფერადი კოსტიუმებით, აღმამაღლებელი მუსიკითა და ინტერაქტიული ცეკვის გამოცდილებით.",
        "February 10-11, 2018": "10-11 თებერვალი, 2018",
        "2:00 PM - 5:00 PM": "14:00 - 17:00",
        "Lincoln Center, NYC": "ლინკოლნ ცენტრი, ნიუ-იორკი",
        "$25 - $80": "$25 - $80",
        "Get Family Tickets": "ოჯახური ბილეთების მიღება",
        "Services": "სერვისები",
        "Our Core Services": "ჩვენი ძირითადი სერვისები",
        "We offer a comprehensive suite of services to drive your business forward.": "ჩვენ გთავაზობთ სერვისების სრულ სპექტრს თქვენი ბიზნესის წინსვლისთვის.",
        "Software Development": "პროგრამული უზრუნველყოფის შექმნა",
        "Custom software solutions tailored to your unique business needs, ensuring efficiency and scalability.": "მორგებული პროგრამული უზრუნველყოფის გადაწყვეტილებები თქვენი უნიკალური ბიზნეს საჭიროებებისთვის, ეფექტურობისა და მასშტაბურობის უზრუნველყოფით.",
        "Digital Strategy & Marketing": "ციფრული სტრატეგია და მარკეტინგი",
        "Driving growth through data-backed digital strategies, SEO, content, and social media management.": "ზრდის ხელშეწყობა მონაცემებზე დაფუძნებული ციფრული სტრატეგიებით, SEO-თი, კონტენტითა და სოციალური მედიის მართვით.",
        "UI/UX Design & Branding": "UI/UX დიზაინი და ბრენდინგი",
        "Creating intuitive and engaging user experiences and developing strong, memorable brand identities.": "ინტუიციური და მიმზიდველი მომხმარებლის გამოცდილების შექმნა და ძლიერი, დასამახსოვრებელი ბრენდის იდენტობების განვითარება.",
        "Cloud & Infrastructure": "ღრუბლოვანი და ინფრასტრუქტურული სერვისები",
        "Modernizing your IT infrastructure with secure, scalable cloud solutions and robust managed services.": "თქვენი IT ინფრასტრუქტურის მოდერნიზაცია უსაფრთხო, მასშტაბირებადი ღრუბლოვანი გადაწყვეტილებებით და მართული სერვისებით.",
        "Learn More": "გაიგე მეტი"
    },
    ru: {
        "Home": "Главная",
        "Projects": "Проекты",
        "Partners": "Партнеры",
        "Contact": "Контакт",
        "The Annual Conference 2017": "Ежегодная конференция 2017",
        "December 20-29, 2017": "20-29 декабря, 2017",
        "We are appearing with something amazing so stay updated": "Мы появляемся с чем-то удивительным, так что следите за обновлениями",
        "Day": "День",
        "hr": "ч",
        "min": "мин",
        "sec": "сек",
        "EVENT SHOW": "ПОКАЗ СОБЫТИЙ",
        "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed diam nonummy nibh euismod tincidunt ut laoreet dolore magna aliquam erat volutpat.": "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed diam nonummy nibh euismod tincidunt ut laoreet dolore magna aliquam erat volutpat.",
        "New York: Christmas Event Live Music": "Нью-Йорк: Рождественский концерт живой музыки",
        "New York: Exclusive Event DJ Party": "Нью-Йорк: Эксклюзивная DJ вечеринка",
        "New York: Children Event Dance Show": "Нью-Йорк: Детское танцевальное шоу",
        "REGISTRATION NOW": "РЕГИСТРАЦИЯ СЕЙЧАС",
        "Your Name": "Ваше имя",
        "Your Email": "Ваш Email",
        "Your Phone": "Ваш телефон",
        "Register Now": "Зарегистрироваться",
        "OUR GREAT SPONSORS": "НАШИ ВЕЛИКИЕ СПОНСОРЫ",
        "GET IN TOUCH WITH US": "СВЯЗАТЬСЯ С НАМИ",
        "Full Name*": "Полное имя*",
        "Email*": "Email*",
        "Phone*": "Телефон*",
        "Subject*": "Тема*",
        "Message*": "Сообщение*",
        "Send Now": "Отправить",
        "Blog": "Блог",
        "Tickets": "Билеты",
        "FAQ": "FAQ",
        "Press": "Пресса",
        "All Rights Reserved": "Все права защищены",
        "Designed By": "Разработано",
        "TEST": "ТЕСТ",
        "First Partner: Full Name": "Первый партнер: Полное имя",
        "Work Gallery": "Рабочая галерея",
        "Test Button": "Тестовая кнопка",
        "Work Hours - 9:00 - 18:00": "Рабочие часы - 9:00 - 18:00",
        "Azerbaijan, Baku": "Азербайджан, Баку",
        "October 20-30, 2025": "20-30 октября, 2025",
        "$75 000 - $200 000": "$75 000 - $200 000",
        "FAMILY DANCE SHOW": "СЕМЕЙНОЕ ТАНЦЕВАЛЬНОЕ ШОУ",
        "New York: Children Event Dance Show": "Нью-Йорк: Детское танцевальное шоу",
        "A magical dance extravaganza designed especially for children and families! Join us for an enchanting performance featuring colorful costumes, uplifting music, and interactive dance experiences.": "Волшебное танцевальное представление, созданное специально для детей и семей! Присоединяйтесь к нам на очаровательное выступление с красочными костюмами, вдохновляющей музыкой и интерактивными танцевальными впечатлениями.",
        "February 10-11, 2018": "10-11 февраля, 2018",
        "2:00 PM - 5:00 PM": "14:00 - 17:00",
        "Lincoln Center, NYC": "Линкольн-центр, Нью-Йорк",
        "$25 - $80": "$25 - $80",
        "Get Family Tickets": "Получить семейные билеты",
        "Services": "Услуги",
        "Our Core Services": "Наши Основные Услуги",
        "We offer a comprehensive suite of services to drive your business forward.": "Мы предлагаем полный набор услуг для продвижения вашего бизнеса.",
        "Software Development": "Разработка ПО",
        "Custom software solutions tailored to your unique business needs, ensuring efficiency and scalability.": "Индивидуальные программные решения, адаптированные к вашим уникальным потребностям, обеспечивающие эффективность и масштабируемость.",
        "Digital Strategy & Marketing": "Цифровая Стратегия и Маркетинг",
        "Driving growth through data-backed digital strategies, SEO, content, and social media management.": "Содействие росту с помощью цифровых стратегий, основанных на данных, SEO, контента и управления социальными сетями.",
        "UI/UX Design & Branding": "UI/UX Дизайн и Брендинг",
        "Creating intuitive and engaging user experiences and developing strong, memorable brand identities.": "Создание интуитивно понятного и привлекательного пользовательского опыта и разработка сильных, запоминающихся брендовых идентичностей.",
        "Cloud & Infrastructure": "Облачные и Инфраструктурные Услуги",
        "Modernizing your IT infrastructure with secure, scalable cloud solutions and robust managed services.": "Модернизация вашей ИТ-инфраструктуры с помощью безопасных, масштабируемых облачных решений и надежных управляемых сервисов.",
        "Learn More": "Подробнее"
    }
};

// Language Persistence - Add this to your main.js file
// Replace the existing language switching section with this code

// Current language - load from localStorage or default to 'en'
let currentLanguage = localStorage.getItem('selectedLanguage') || 'en';

// DOM elements
const hamburger = document.querySelector('.hamburger');
const navMenu = document.querySelector('.nav-menu');
const langButtons = document.querySelectorAll('.lang-btn');
const navbar = document.querySelector('.navbar');

// Set initial language on page load
document.addEventListener('DOMContentLoaded', function () {
    // Apply saved language
    applyLanguage(currentLanguage);

    // Update active button
    langButtons.forEach(btn => {
        if (btn.dataset.lang === currentLanguage) {
            btn.classList.add('active');
        } else {
            btn.classList.remove('active');
        }
    });
});

// Language switching
langButtons.forEach(btn => {
    btn.addEventListener('click', () => {
        // Remove active class from all buttons
        langButtons.forEach(b => b.classList.remove('active'));
        // Add active class to clicked button
        btn.classList.add('active');

        // Set current language
        currentLanguage = btn.dataset.lang;

        // Save to localStorage
        localStorage.setItem('selectedLanguage', currentLanguage);

        // Apply language
        applyLanguage(currentLanguage);
    });
});

// Apply language function
function applyLanguage(lang) {
    // Update elements with data attributes
    document.querySelectorAll('[data-' + lang + ']').forEach(element => {
        const text = element.getAttribute('data-' + lang);
        if (text) {
            element.textContent = text;
        }
    });

    // Update placeholders
    document.querySelectorAll('[data-' + lang + '-placeholder]').forEach(element => {
        const placeholder = element.getAttribute('data-' + lang + '-placeholder');
        if (placeholder) {
            element.placeholder = placeholder;
        }
    });
}

// Update the existing updateLanguage function to use applyLanguage
function updateLanguage() {
    applyLanguage(currentLanguage);
}

// Mobile menu toggle
hamburger.addEventListener('click', () => {
    hamburger.classList.toggle('active');
    navMenu.classList.toggle('active');

    // Animate hamburger
    const spans = hamburger.querySelectorAll('span');
    if (hamburger.classList.contains('active')) {
        spans[0].style.transform = 'rotate(-45deg) translate(-5px, 6px)';
        spans[1].style.opacity = '0';
        spans[2].style.transform = 'rotate(45deg) translate(-5px, -6px)';
    } else {
        spans[0].style.transform = 'none';
        spans[1].style.opacity = '1';
        spans[2].style.transform = 'none';
    }
});

// Close mobile menu when clicking on nav links
document.querySelectorAll('.nav-list a').forEach(link => {
    link.addEventListener('click', () => {
        hamburger.classList.remove('active');
        navMenu.classList.remove('active');

        const spans = hamburger.querySelectorAll('span');
        spans[0].style.transform = 'none';
        spans[1].style.opacity = '1';
        spans[2].style.transform = 'none';
    });
});

// Navbar background on scroll
window.addEventListener('scroll', () => {
    if (window.scrollY > 100) {
        navbar.style.background = 'rgba(0, 0, 0, 0.95)';
    } else {
        navbar.style.background = 'rgba(0, 0, 0, 0.9)';
    }
});

// Language switching
langButtons.forEach(btn => {
    btn.addEventListener('click', () => {
        // Remove active class from all buttons
        langButtons.forEach(b => b.classList.remove('active'));
        // Add active class to clicked button
        btn.classList.add('active');

        // Set current language
        currentLanguage = btn.dataset.lang;
        updateLanguage();
    });
});

// Update language function
function updateLanguage() {
    // Update elements with data attributes
    document.querySelectorAll('[data-' + currentLanguage + ']').forEach(element => {
        const text = element.getAttribute('data-' + currentLanguage);
        element.textContent = text;
    });

    // Update placeholders
    document.querySelectorAll('[data-' + currentLanguage + '-placeholder]').forEach(element => {
        const placeholder = element.getAttribute('data-' + currentLanguage + '-placeholder');
        element.placeholder = placeholder;
    });
}

// Countdown timer
function updateCountdown() {
    // Set target date (you can change this to any future date)
    const targetDate = new Date('2024-12-25 00:00:00').getTime();
    const now = new Date().getTime();
    const distance = targetDate - now;

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    // Update countdown display
    const daysEl = document.getElementById('days');
    const hoursEl = document.getElementById('hours');
    const minutesEl = document.getElementById('minutes');
    const secondsEl = document.getElementById('seconds');

    if (daysEl) daysEl.textContent = days.toString().padStart(2, '0');
    if (hoursEl) hoursEl.textContent = hours.toString().padStart(2, '0');
    if (minutesEl) minutesEl.textContent = minutes.toString().padStart(2, '0');
    if (secondsEl) secondsEl.textContent = seconds.toString().padStart(2, '0');

    // If countdown finished
    if (distance < 0) {
        if (daysEl) daysEl.textContent = '00';
        if (hoursEl) hoursEl.textContent = '00';
        if (minutesEl) minutesEl.textContent = '00';
        if (secondsEl) secondsEl.textContent = '00';
    }
}

// Update countdown every second
setInterval(updateCountdown, 1000);
updateCountdown(); // Initial call

// Smooth scrolling for anchor links
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            const headerOffset = 80;
            const elementPosition = target.getBoundingClientRect().top;
            const offsetPosition = elementPosition + window.pageYOffset - headerOffset;

            window.scrollTo({
                top: offsetPosition,
                behavior: 'smooth'
            });
        }
    });
});

// Scroll reveal animations
const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
};

const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('animate');
        }
    });
}, observerOptions);

// Add scroll animations to elements
window.addEventListener('load', () => {
    // Add animate-on-scroll class to elements
    const animateElements = document.querySelectorAll('.project-card, .sponsor-card, .section-title, .section-description');
    animateElements.forEach(el => {
        el.classList.add('animate-on-scroll');
        observer.observe(el);
    });
});

// Form handling
document.querySelector('.registration-form')?.addEventListener('submit', function (e) {
    e.preventDefault();

    // Get form data
    const formData = new FormData(this);
    const name = this.querySelector('input[type="text"]').value;
    const email = this.querySelector('input[type="email"]').value;
    const phone = this.querySelector('input[type="tel"]').value;

    // Simple validation
    if (!name || !email || !phone) {
        alert(currentLanguage === 'en' ? 'Please fill in all fields' :
            currentLanguage === 'ka' ? 'გთხოვთ შეავსოთ ყველა ველი' :
                'Пожалуйста, заполните все поля');
        return;
    }

    // Email validation
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
        alert(currentLanguage === 'en' ? 'Please enter a valid email address' :
            currentLanguage === 'ka' ? 'გთხოვთ შეიყვანოთ სწორი ელ-ფოსტის მისამართი' :
                'Пожалуйста, введите действительный адрес электронной почты');
        return;
    }

    // Show success message
    alert(currentLanguage === 'en' ? 'Registration successful! We will contact you soon.' :
        currentLanguage === 'ka' ? 'რეგისტრაცია წარმატებული! ჩვენ მალე დაგიკავშირდებით.' :
            'Регистрация прошла успешно! Мы свяжемся с вами в ближайшее время.');

    // Reset form
    this.reset();

    // Add submission animation
    const button = this.querySelector('button');
    button.style.transform = 'scale(0.95)';
    setTimeout(() => {
        button.style.transform = 'scale(1)';
    }, 200);
});

document.querySelector('.contact-form')?.addEventListener('submit', function (e) {
    e.preventDefault();

    // Get form data
    const name = this.querySelector('input[placeholder*="Name"]').value;
    const email = this.querySelector('input[type="email"]').value;
    const phone = this.querySelector('input[type="tel"]').value;
    const subject = this.querySelector('input[placeholder*="Subject"]').value;
    const message = this.querySelector('textarea').value;

    // Simple validation
    if (!name || !email || !phone || !subject || !message) {
        alert(currentLanguage === 'en' ? 'Please fill in all fields' :
            currentLanguage === 'ka' ? 'გთხოვთ შეავსოთ ყველა ველი' :
                'Пожалуйста, заполните все поля');
        return;
    }

    // Email validation
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
        alert(currentLanguage === 'en' ? 'Please enter a valid email address' :
            currentLanguage === 'ka' ? 'გთხოვთ შეიყვანოთ სწორი ელ-ფოსტის მისამართი' :
                'Пожалуйста, введите действительный адрес электронной почты');
        return;
    }

    // Show success message
    alert(currentLanguage === 'en' ? 'Message sent successfully! We will get back to you soon.' :
        currentLanguage === 'ka' ? 'შეტყობინება წარმატებით გაიგზავნა! ჩვენ მალე დაგიკავშირდებით.' :
            'Сообщение отправлено успешно! Мы скоро свяжемся с вами.');

    // Reset form
    this.reset();

    // Add submission animation
    const button = this.querySelector('button');
    button.style.transform = 'scale(0.95)';
    setTimeout(() => {
        button.style.transform = 'scale(1)';
    }, 200);
});

// Parallax effect for hero section
window.addEventListener('scroll', () => {
    const scrolled = window.pageYOffset;
    const hero = document.querySelector('.hero');
    if (hero) {
        const rate = scrolled * -0.5;
        hero.style.transform = `translateY(${rate}px)`;
    }
});

// Add floating animation to project cards
document.querySelectorAll('.project-card').forEach((card, index) => {
    card.style.animationDelay = `${index * 0.2}s`;
    card.addEventListener('mouseenter', function () {
        this.style.transform = 'translateY(-20px) scale(1.02)';
    });

    card.addEventListener('mouseleave', function () {
        this.style.transform = 'translateY(0) scale(1)';
    });
});

// Add hover effect to sponsor cards
document.querySelectorAll('.sponsor-card').forEach((card, index) => {
    card.style.animationDelay = `${index * 0.1}s`;

    card.addEventListener('mouseenter', function () {
        this.style.transform = 'translateY(-15px) rotate(2deg)';
    });

    card.addEventListener('mouseleave', function () {
        this.style.transform = 'translateY(0) rotate(0deg)';
    });
});

// Add ripple effect to buttons
function addRippleEffect(button) {
    button.addEventListener('click', function (e) {
        const ripple = document.createElement('span');
        const rect = this.getBoundingClientRect();
        const size = Math.max(rect.width, rect.height);
        const x = e.clientX - rect.left - size / 2;
        const y = e.clientY - rect.top - size / 2;

        ripple.style.cssText = `
            position: absolute;
            border-radius: 50%;
            transform: scale(0);
            animation: ripple 0.6s linear;
            background-color: rgba(255, 255, 255, 0.5);
            left: ${x}px;
            top: ${y}px;
            width: ${size}px;
            height: ${size}px;
            pointer-events: none;
        `;

        this.style.position = 'relative';
        this.style.overflow = 'hidden';
        this.appendChild(ripple);

        setTimeout(() => {
            ripple.remove();
        }, 600);
    });
}

// Add ripple effect to all buttons
document.querySelectorAll('button').forEach(addRippleEffect);

// Add CSS for ripple animation
const style = document.createElement('style');
style.textContent = `
    @keyframes ripple {
        to {
            transform: scale(4);
            opacity: 0;
        }
    }
`;
document.head.appendChild(style);

// Loading animation
window.addEventListener('load', () => {
    document.body.style.opacity = '0';
    setTimeout(() => {
        document.body.style.transition = 'opacity 0.5s ease';
        document.body.style.opacity = '1';
    }, 100);
});

// Add typing effect to hero title
function typeWriter(element, text, speed = 100) {
    element.textContent = '';
    let i = 0;

    function type() {
        if (i < text.length) {
            element.textContent += text.charAt(i);
            i++;
            setTimeout(type, speed);
        }
    }

    type();
}

// Initialize typing effect after page load
window.addEventListener('load', () => {
    setTimeout(() => {
        const heroTitle = document.querySelector('.hero-title');
        if (heroTitle) {
            const originalText = heroTitle.textContent;
            typeWriter(heroTitle, originalText, 50);
        }
    }, 500);
});

// Add scroll progress indicator
const scrollProgress = document.createElement('div');
scrollProgress.style.cssText = `
    position: fixed;
    top: 0;
    left: 0;
    width: 0%;
    height: 3px;
    background: linear-gradient(90deg, #ff6b35, #ff9a56);
    z-index: 10000;
    transition: width 0.1s ease;
`;
document.body.appendChild(scrollProgress);

window.addEventListener('scroll', () => {
    const scrolled = (window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100;
    scrollProgress.style.width = scrolled + '%';
});

// Add custom cursor effect
const cursor = document.createElement('div');
cursor.style.cssText = `
    position: fixed;
    width: 20px;
    height: 20px;
    background: #ff6b35;
    border-radius: 50%;
    pointer-events: none;
    z-index: 10000;
    transition: transform 0.1s ease;
    opacity: 0;
`;
document.body.appendChild(cursor);

document.addEventListener('mousemove', (e) => {
    cursor.style.left = e.clientX - 10 + 'px';
    cursor.style.top = e.clientY - 10 + 'px';
    cursor.style.opacity = '0.7';
});

document.addEventListener('mouseleave', () => {
    cursor.style.opacity = '0';
});

// Add hover effect to interactive elements
document.querySelectorAll('a, button, .project-card, .sponsor-card').forEach(element => {
    element.addEventListener('mouseenter', () => {
        cursor.style.transform = 'scale(2)';
        cursor.style.background = '#e55a2e';
    });

    element.addEventListener('mouseleave', () => {
        cursor.style.transform = 'scale(1)';
        cursor.style.background = '#ff6b35';
    });
});

// Initialize everything when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    // Set initial language
    updateLanguage();

    // Start animations
    setTimeout(() => {
        document.querySelectorAll('.animate-on-scroll').forEach(el => {
            if (isElementInViewport(el)) {
                el.classList.add('animate');
            }
        });
    }, 500);
});

// Helper function to check if element is in viewport
function isElementInViewport(el) {
    const rect = el.getBoundingClientRect();
    return (
        rect.top >= 0 &&
        rect.left >= 0 &&
        rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
        rect.right <= (window.innerWidth || document.documentElement.clientWidth)
    );
}

// Add performance optimization for mobile
if (window.innerWidth < 768) {
    // Disable parallax on mobile for better performance
    window.removeEventListener('scroll', () => {
        const scrolled = window.pageYOffset;
        const hero = document.querySelector('.hero');
        if (hero) {
            const rate = scrolled * -0.5;
            hero.style.transform = `translateY(${rate}px)`;
        }
    });

    // Reduce animation complexity on mobile
    document.querySelectorAll('.project-card, .sponsor-card').forEach(card => {
        card.style.transition = 'transform 0.2s ease';
    });
}

document.querySelectorAll('.showcase-card').forEach(card => {
    card.addEventListener('mouseenter', () => {
        card.style.transition = 'transform 0.3s ease, box-shadow 0.3s ease';
        card.style.transform = 'translateY(-12px) scale(1.05)';
    });
    card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0) scale(1)';
    });
});


// Odometer Animation Script
document.addEventListener('DOMContentLoaded', function () {
    const statNumbers = document.querySelectorAll('.stat-number');
    let animated = false;

    // Function to animate counter
    function animateCounter(element) {
        const target = parseInt(element.getAttribute('data-target'));
        const duration = 2500; // 2.5 seconds
        const increment = target / (duration / 16); // 60 FPS
        let current = 0;

        element.closest('.stat-card').classList.add('counting');

        const timer = setInterval(() => {
            current += increment;
            if (current >= target) {
                element.textContent = target;
                clearInterval(timer);
                element.closest('.stat-card').classList.remove('counting');
            } else {
                element.textContent = Math.floor(current);
            }
        }, 16);
    }

    // Intersection Observer for triggering animation
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting && !animated) {
                animated = true;
                statNumbers.forEach((number, index) => {
                    setTimeout(() => {
                        animateCounter(number);
                    }, index * 200); // Stagger animation
                });
            }
        });
    }, {
        threshold: 0.5
    });

    // Observe the stats section
    const statsSection = document.querySelector('.stats-section');
    if (statsSection) {
        observer.observe(statsSection);
    }
});

const currentPage = window.location.pathname.split("/").pop();

document.querySelectorAll(".nav-list a").forEach(link => {
    const linkPage = link.getAttribute("href");

    // ignore hash links (#contact)
    if (linkPage.startsWith("#")) return;

    if (linkPage === currentPage) {
        link.classList.add("active");
    }
});

// CopyEmail Script
function copyEmail(email) {
    navigator.clipboard.writeText(email)
        .then(() => {
            alert("Copied: " + email);
        })
        .catch(err => {
            console.error("Copy failed", err);
        });
};


document.addEventListener("DOMContentLoaded", () => {
    const texts = {
        en: "Industrial building structures\nDesign - Import - Installation",
        ka: "ინდუსტრიული შენობა ნაგებობების\nპროექტირება - იმპორტი - ინსტალაცია",
        ru: "Проектирование промышленных\nсооружений - Импорт - Монтаж"
    };

    const typingElement = document.getElementById("hero-typing-text");
    const cursor = document.querySelector(".terminal-cursor");

    let index = 0;
    let currentText = "";
    let typingTimeout;

    function startTyping(text) {
        clearTimeout(typingTimeout);
        typingElement.innerHTML = "";
        index = 0;
        currentText = text;

        function typeEffect() {
            if (index < currentText.length) {
                if (currentText[index] === "\n") {
                    typingElement.innerHTML += "<br>";
                } else {
                    typingElement.innerHTML += currentText[index];
                }
                index++;
                typingTimeout = setTimeout(typeEffect, 55);
            }
        }

        typeEffect();
    }

    // 🔹 საწყისი ენა (active ღილაკიდან)
    const activeLangBtn = document.querySelector(".lang-btn.active");
    const initialLang = activeLangBtn ? activeLangBtn.dataset.lang : "ka";
    startTyping(texts[initialLang]);

    // 🔹 ენის შეცვლაზე თავიდან ატრიალებს typing-ს
    document.querySelectorAll(".lang-btn").forEach(btn => {
        btn.addEventListener("click", () => {
            const selectedLang = btn.dataset.lang;
            if (texts[selectedLang]) {
                startTyping(texts[selectedLang]);
            }
        });
    });
});

<!-- YouTube Video Autoplay on Scroll Script -->

// YouTube Video Autoplay on Scroll
document.addEventListener('DOMContentLoaded', function () {
    const videoWrapper = document.getElementById('videoWrapper');
    const youtubeIframe = document.getElementById('youtubeVideo');

    // Function to check if element is in viewport
    function isInViewport(element) {
        const rect = element.getBoundingClientRect();
        return (
            rect.top <= (window.innerHeight || document.documentElement.clientHeight) &&
            rect.bottom >= 0
        );
    }

    // Function to play video when in viewport
    function checkAndPlayVideo() {
        if (isInViewport(videoWrapper)) {
            // Send play command to YouTube iframe
            youtubeIframe.contentWindow.postMessage('{"event":"command","func":"playVideo","args":""}', '*');
            window.removeEventListener('scroll', checkAndPlayVideo);
        }
    }

    // Listen for scroll events
    window.addEventListener('scroll', checkAndPlayVideo);

    // Also check on initial load
    checkAndPlayVideo();
});